name: totp-slack-command

services:
  lint:
    image: "golangci/golangci-lint:v2.12.2"
    entrypoint: ["golangci-lint", "run", "--fix"]
    working_dir: /app
    volumes:
      - ".:/app"
      - "gomod_cache:/go/pkg/mod"
      - "lint_cache:/root/.cache"

volumes:
  gomod_cache:
  lint_cache:
